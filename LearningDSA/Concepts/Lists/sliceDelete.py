myList = ['a', 'b', 'c', 'd', 'e', 'f']

# lets say we want to print the first two elements of the list, we can do it using slice operator

print(myList[:2])
print(myList[1:])
print(myList[:])      # if we specify no upper bound and lower bound, al the values in the list are taken